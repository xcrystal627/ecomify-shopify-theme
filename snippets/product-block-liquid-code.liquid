{% liquid 
    assign mt = block.settings.mt | prepend: 'mt-'
    assign mb = block.settings.mb | prepend: 'mb-'
%}

<div class="product-block-liquid-code {{ mt }} {{ mb }}" {{ block.shopify_attributes }}>
    {{ block.settings.liquid }}
</div>